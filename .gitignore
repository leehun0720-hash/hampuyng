node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts.bak

# 런타임 데이터 — 최초 실행 시 src/lib/seed 에서 자동 생성된다.
# settings.json 에는 API 키가 저장되므로 특히 커밋하면 안 된다.
data/

# 원본 사업제안서 — 미제출 상태의 대외 문서이므로 공개 저장소에 올리지 않는다.
*.pdf

.env*
.DS_Store
